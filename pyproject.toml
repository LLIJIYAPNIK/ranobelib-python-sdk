[project]
name = "ranobelib-python-sdk"
version = "0.3.0"
description = "Async Python SDK for ranobelib.me: title metadata, chapter/volume downloads, and export to epub/fb2/txt/pdf"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "LLIJIYAPNIK", email = "kas4wot@yandex.ru" }
]
keywords = ["ranobelib", "ranobe", "novel", "webnovel", "epub", "fb2", "sdk"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "ebooklib>=0.20",
    "httpx>=0.27",
    "pydantic>=2.7",
    "rich>=15.0.0",
    "weasyprint>=69.0",
]

[project.urls]
Homepage = "https://github.com/LLIJIYAPNIK/ranobelib-python-sdk"
Repository = "https://github.com/LLIJIYAPNIK/ranobelib-python-sdk"
Issues = "https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues"
Changelog = "https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/blob/main/CHANGELOG.md"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "ranobelib"

[dependency-groups]
dev = [
    "coveralls>=4.0",
    "lxml>=5.0",
    "mypy>=1.10",
    "pypdf>=6.14.2",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "pytest-recording>=0.13",
    "ruff>=0.6",
    "vcrpy>=6.0",
]
docs = [
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.25",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src"]
extend-exclude = ["CLAUDE.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "C4"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src"]

[[tool.mypy.overrides]]
# ebooklib and weasyprint ship no type stubs / py.typed marker.
module = ["ebooklib", "ebooklib.*", "weasyprint", "weasyprint.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["ranobelib"]

[tool.coverage.report]
fail_under = 95
exclude_also = [
    "if TYPE_CHECKING:",
    "\\.\\.\\.",
]
